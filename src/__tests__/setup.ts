import '@testing-library/jest-dom';
import { beforeAll, afterAll } from 'vitest';

// テスト環境のグローバルセットアップ
beforeAll(() => {
  // テスト全体で共通の初期化処理があればここに記述
});

afterAll(() => {
  // テスト全体で共通のクリーンアップ処理があればここに記述
}); 